package templates

import (
	"cmp"
	"encoding/json"
	"github.com/ottrec/website/internal/asset"
	"github.com/ottrec/website/pkg/ottrecidx"
	"github.com/ottrec/website/static"
	"strings"
	"time"
)

type WebsiteParams struct {
	Base string
	Data ottrecidx.DataRef
}

// websitePageMeta configures the shared page chrome rendered by [websitePage].
type websitePageMeta struct {
	Base        string              // absolute site root (params.Base); "" omits the canonical link and social meta
	Path        string              // canonical path under Base ("" for the homepage)
	Title       string              // full <title>, including any site suffix
	Desc        string              // meta description ("" omits the tag)
	SocialDesc  string              // og/twitter description when it should differ from Desc
	Article     bool                // og:type article instead of website
	Author      string              // article author ("" omits the meta)
	DateISO     string              // article published date ("" omits the meta)
	NoIndex     bool                // robots noindex (error pages)
	NoSocial    bool                // omit the social meta even with a Base (the data site)
	BodyClass   string              // class for <body>
	Nav         string              // active topnav slug ("" for none)
	Updated     time.Time           // dataset timestamp for the topnav (zero to omit)
	TopNav      templ.Component     // overrides the default topnav (the data site)
	CSS         []*asset.Asset      // page stylesheets, linked after the shared website.css
	Breadcrumbs []websiteBreadcrumb // BreadcrumbList JSON-LD trail (nil to omit)
}

// websiteBreadcrumb is one crumb in a page's BreadcrumbList JSON-LD.
type websiteBreadcrumb struct {
	Name string
	Path string // path under Base
}

// websiteLDJSON returns the page's schema.org JSON-LD: a WebPage carrying the
// dataset's dateModified (a freshness signal that keeps the volatile date out of
// the visible snippet), plus a BreadcrumbList when crumbs are set. Empty for
// pages with no canonical base or that are noindex.
func websiteLDJSON(meta websitePageMeta) string {
	if meta.Base == "" || meta.NoIndex {
		return ""
	}
	page := map[string]any{"@type": "WebPage", "url": meta.Base + meta.Path}
	if meta.Title != "" {
		page["name"] = meta.Title
	}
	if !meta.Updated.IsZero() {
		page["dateModified"] = meta.Updated.In(ottrecidx.TZ).Format(time.RFC3339)
	}
	graph := []map[string]any{page}
	if len(meta.Breadcrumbs) > 0 {
		items := make([]map[string]any, len(meta.Breadcrumbs))
		for i, b := range meta.Breadcrumbs {
			items[i] = map[string]any{"@type": "ListItem", "position": i + 1, "name": b.Name, "item": meta.Base + b.Path}
		}
		graph = append(graph, map[string]any{"@type": "BreadcrumbList", "itemListElement": items})
	}
	buf, err := json.Marshal(map[string]any{"@context": "https://schema.org", "@graph": graph})
	if err != nil {
		return ""
	}
	return string(buf)
}

// ogType returns the og:type for the social meta.
func (m websitePageMeta) ogType() string {
	if m.Article {
		return "article"
	}
	return "website"
}

// websitePage renders the chrome shared by every page: the head, the topnav,
// and the body wrapper. Children render the rest of the body (the main
// content, the footer, and any scripts).
templ websitePage(meta websitePageMeta) {
	<!DOCTYPE html>
	<html lang="en">
		<head>
			<meta charset="utf-8"/>
			<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
			@websiteThemeHead()
			@websiteIconsHead()
			if meta.NoIndex {
				<meta name="robots" content="noindex"/>
			}
			if meta.Base != "" {
				<link rel="canonical" href={ meta.Base + meta.Path }/>
			}
			<title>{ meta.Title }</title>
			if meta.Desc != "" {
				<meta name="description" content={ meta.Desc }/>
			}
			if meta.Author != "" {
				<meta name="author" content={ meta.Author }/>
			}
			if meta.DateISO != "" {
				<meta name="date" content={ meta.DateISO }/>
			}
			if !meta.NoSocial {
				@websiteSocialHead(meta)
			}
			if ld := websiteLDJSON(meta); ld != "" {
				<script type="application/ld+json">@templ.Raw(ld)</script>
			}
			<link rel="stylesheet" href={ static.Path(static.WebsiteCSS) }/>
			for _, a := range meta.CSS {
				<link rel="stylesheet" href={ static.Path(a) }/>
			}
			@websiteHeadExtra()
		</head>
		<body class={ meta.BodyClass }>
			if meta.TopNav != nil {
				@meta.TopNav
			} else {
				@websiteTopNav(meta.Nav, meta.Updated)
			}
			{ children... }
		</body>
	</html>
}

// websiteHeadExtra renders the raw HTML set by [SetHeadExtra], at the bottom
// of every page's head.
templ websiteHeadExtra() {
	if headExtra != nil {
		@headExtra
	}
}

// websiteAboutExtra renders the raw HTML set by [SetAboutExtra], at the bottom
// of every page's head.
templ websiteAboutExtra() {
	if aboutExtra != nil {
		@aboutExtra
	}
}

// websiteHomeExtra renders the raw HTML set by [SetHomeExtra], at the bottom of
// the homepage's main content.
templ websiteHomeExtra() {
	if homeExtra != nil {
		<div class="home-extra">
			@homeExtra
		</div>
	}
}

// websiteMapTiles emits the basemap tile configuration as a JSON island, read
// by static/tiles.ts. It must precede any map script.
templ websiteMapTiles() {
	@templ.JSONScript("map-tiles", mapTiles)
}

// websiteThemeHead applies the color scheme override saved by the navbar
// toggle. The inline script (the compiled theme-apply.ts, shared with the
// navbar toggle in theme.ts) must run before first paint to avoid flashing the
// wrong theme.
templ websiteThemeHead() {
	@templ.Raw("<script>" + static.InlineJS(static.ThemeApplyJS) + "</script>")
	<script src={ static.Path(static.ThemeJS) } defer></script>
}

// websiteIconsHead links the site icons. The ico fallback is also reachable
// at /favicon.ico for clients that never read the page head.
templ websiteIconsHead() {
	<link rel="icon" href={ static.Path(static.FaviconICO) + "?v=1" } sizes="32x32"/>
	<link rel="icon" href={ static.Path(static.FaviconSVG) + "?v=1" } type="image/svg+xml"/>
	<link rel="apple-touch-icon" href={ static.Path(static.AppleTouchIconPNG) + "?v=1" }/>
}

// websiteSocialHead emits Open Graph and Twitter Card meta for link previews.
// The title and description mirror the page's <title> and <meta
// name="description"> unless SocialDesc overrides the latter. It is a no-op
// without meta.Base, since og:url and og:image must be absolute. The share
// image is generated by static/social-card.py.
templ websiteSocialHead(meta websitePageMeta) {
	if meta.Base != "" {
		{{ img := strings.TrimSuffix(meta.Base, "/") + static.Path(static.SocialCardPNG) }}
		{{ desc := cmp.Or(meta.SocialDesc, meta.Desc) }}
		<meta property="og:type" content={ meta.ogType() }/>
		<meta property="og:site_name" content="ottrec.ca"/>
		<meta property="og:locale" content="en_CA"/>
		<meta property="og:title" content={ meta.Title }/>
		<meta property="og:description" content={ desc }/>
		<meta property="og:url" content={ meta.Base + meta.Path }/>
		<meta property="og:image" content={ img }/>
		<meta property="og:image:type" content="image/png"/>
		<meta property="og:image:width" content="1200"/>
		<meta property="og:image:height" content="630"/>
		<meta property="og:image:alt" content="ottrec.ca — Ottawa recreation schedules, in one place."/>
		if meta.Author != "" {
			<meta property="article:author" content={ meta.Author }/>
		}
		if meta.DateISO != "" {
			<meta property="article:published_time" content={ meta.DateISO }/>
		}
		<meta name="twitter:card" content="summary_large_image"/>
		<meta name="twitter:title" content={ meta.Title }/>
		<meta name="twitter:description" content={ desc }/>
		<meta name="twitter:image" content={ img }/>
	}
}

// websiteTopNav renders the shared navbar. active is the slug of the active
// nav link ("" for none), and updated is the data timestamp (zero to omit,
// e.g., when the data isn't available).
templ websiteTopNav(active string, updated time.Time) {
	<nav class="site-topnav">
		<a class="brand" href="/">ottrec.ca</a>
		<a class={ "nav", templ.KV("active", active == "today") } href="/today">Today</a>
		<a class={ "nav", templ.KV("active", active == "map") } href="/map">Map</a>
		<a class={ "nav", templ.KV("active", active == "schedules") } href="/schedules">Schedules</a>
		<a class={ "nav", templ.KV("active", active == "activities") } href="/activities">Activities</a>
		<a class={ "nav", templ.KV("active", active == "about") } href="/about">About</a>
		<div class="topnav-end">
			<button type="button" class="theme-toggle msym" id="theme-toggle" data-theme="auto" title="Toggle color scheme" hidden></button>
			if !updated.IsZero() {
				<time datetime={ localRFC3339(updated) }>updated<br/>{ localFormat(updated, "2006-01-02 15:04") }</time>
			}
		</div>
	</nav>
}

// facStar renders the facility star toggle shared by the schedules, today, and
// activities pages, revealed and wired up by starred.js.
templ facStar(slug string) {
	<button type="button" class="fac-star msym" data-fac-star={ slug } aria-pressed="false" aria-label="Star facility" hidden></button>
}

// localTime renders a <time> for t in the schedule timezone.
templ localTime(t time.Time, layout string) {
	<time datetime={ localRFC3339(t) }>{ localFormat(t, layout) }</time>
}

// websiteFooter renders the shared site footer.
templ websiteFooter() {
	<footer class="site-footer">
		<p>© 2025–2026 Patrick Gaskin.</p>
		<nav>
			<ul>
				<li><a href="/about">About</a></li>
				<li><a href="/about#terms">Terms</a></li>
				<li><a href="/about#data">Data Sources</a></li>
				<li><a href="https://data.ottrec.ca">Dataset</a></li>
				<li><a href="https://github.com/ottrec">GitHub</a></li>
			</ul>
		</nav>
	</footer>
}

templ WebsiteHomePage(params WebsiteParams) {
	{{ updated := params.Data.Index().Updated() }}
	@websitePage(websitePageMeta{
		Base:      params.Base,
		Title:     "Ottawa Recreation Schedules",
		Desc:      "Find drop-in swimming, skating, and more at City of Ottawa recreation facilities. All schedules in one place and facilities on a map, updated daily.",
		BodyClass: "home-page",
		Updated:   updated,
		CSS:       []*asset.Asset{static.HomeCSS},
	}) {
		<main>
			<header class="home-hero">
				<h1>ottrec.ca</h1>
				<p>Find drop-in swimming, skating, and more across every City of Ottawa recreation facility.</p>
				<p class="home-updated">Schedules refreshed daily.</p>
			</header>
			<nav class="home-tiles" aria-label="Browse">
				<a href="/today" data-icon="schedule">
					<h2>This week</h2>
					<p>See what's on over the next week, day by day, including cancellations and notices.</p>
				</a>
				<a href="/map" data-icon="map">
					<h2>Map</h2>
					<p>Every facility on a map, filterable by activity, weekday, and time.</p>
				</a>
				<a href="/schedules" data-icon="calendar_month">
					<h2>All schedules</h2>
					<p>Browse or search every facility's full drop-in schedule on one page.</p>
				</a>
			</nav>
			<h2 class="home-section-label">Browse by activity</h2>
			<nav class="home-tiles home-activities" aria-label="Activities">
				for _, cat := range ScheduleCategories {
					<a href={ templ.URL("/schedules/" + cat.Slug) } data-icon={ cat.Icon }>
						<h2>{ cat.Name }</h2>
					</a>
				}
				<a class="home-activities-more" href="/activities" data-icon="overview">
					<h2>More&hellip;</h2>
				</a>
			</nav>
			<p class="home-advanced"><a href="/about/ottrecql">Advanced search</a></p>
			<section class="home-about">
				<p>ottrec.ca gathers the drop-in recreation schedules from every City of Ottawa facility into one place, so you can browse, search, and compare them all at once. Filter by activity, time, or area, see what's on across the city this week, and find every facility on a map. Updated daily.</p>
			</section>
			//@TempWarning(false)
			<section class="home-other" aria-labelledby="home-other-heading">
				<h2 id="home-other-heading">More stuff</h2>
				<ul>
					<li><a href="https://data.ottrec.ca">dataset</a> &mdash; use the schedule data to build something yourself.</li>
					<li><a href="/about/ottrecql">ottrecql</a> &mdash; info about the advanced search query syntax, for building your own filtered page.</li>
					if page, ok := AboutContentBySlug("regions"); ok {
						<li><a href="/about/regions">{ page.HomeLabel }</a> &mdash; { page.HomeBlurb }</li>
					}
					if page, ok := AboutContentBySlug("data-quality"); ok {
						<li><a href="/about/data-quality">{ page.HomeLabel }</a> &mdash; { page.HomeBlurb }</li>
					}
				</ul>
			</section>
			<section class="home-news" aria-labelledby="home-news-heading">
				<h2 id="home-news-heading">What's new</h2>
				<ul>
					<li>
						<time datetime="2026-07-10">July 10, 2026</time>
						<p>I've added some tips to various parts of the website to make it easier to use.</p>
					</li>
					<li>
						<time datetime="2026-07-08">July 8, 2026</time>
						<p>The activity pages have been completely revamped and now include facility information and a list of upcoming activities.</p>
					</li>
					<li>
						<time datetime="2026-07-07">July 7, 2026</time>
						<p>The <a href="/today">today</a> page now shows more helpful warnings about potential cancellations or time changes.</p>
					</li>
					<li>
						<time datetime="2026-06-15">Jun 15, 2026</time>
						<p>You can now find <a href="/today">upcoming activities</a> over the next week as a chronological timeline, which should be much easier to read on small screens.</p>
					</li>
					<li>
						<time datetime="2026-06-14">Jun 14, 2026</time>
						<p>You can now get a rough idea of where a facility is with the new region/area labels. See <a href="/about/regions">here</a> { "for more info" }.</p>
					</li>
					<li>
						<time datetime="2026-06-13">Jun 13, 2026</time>
						<p>Star facilities to bring them to the top of the page and highlight them on the map.</p>
					</li>
					<li>
						<time datetime="2026-06-12">Jun 12, 2026</time>
						<p>Filter schedules with <a href="/about/ottrecql">custom queries</a> to build your own page with only the facilities and activities you care about.</p>
					</li>
				</ul>
			</section>
			@websiteHomeExtra()
		</main>
		@websiteFooter()
	}
}

// WebsiteErrorPage is used for all website error responses, including 404s
// and data-unavailable errors (so it cannot depend on the data).
templ WebsiteErrorPage(title, message string) {
	@websitePage(websitePageMeta{
		Title:     title,
		NoIndex:   true,
		BodyClass: "error-page",
	}) {
		<main class="error-page">
			<h1>{ title }</h1>
			<p>{ message }</p>
			<p><a href="/">Go to the homepage</a></p>
		</main>
	}
}

// TODO: remove this later, also make it configurable at runtime
templ TempWarning(dismissable bool) {
	<aside class={ "temp-warn", templ.KV("temp-warn-dismissable", dismissable) }>
		@templ.Raw(tempWarnCSS)
		if dismissable {
			<button type="button" class="temp-warn-close" aria-label="Dismiss" onclick="this.closest('.temp-warn').remove()">&times;</button>
		}
		<div class="temp-warn-head">
			<span class="temp-warn-ico" aria-hidden="true"></span>
			<h2>Temporary schedule inaccuracies (July 2026)</h2>
		</div>
		<div class="temp-warn-body">
			<p>As of July 5, 2026, there are a bunch of facilities where the City of Ottawa website for swimming and skating is completely incorrect on some days.</p>
			<p>Since ottrec.ca gets information from the facility webpages, this also affects the times displayed here.</p>
			<p>It is unusual for there to be so many inaccuracies, and I have written to the city about it.</p>
			<p>I strongly recommend calling the facility to confirm skating or swimming hours.</p>
			<p>Some of the corrections for facilities I've been to:</p>
			<ul>
				<li>Bob MacQuarrie has no skating on Sunday, likely for the rest of the summer. <span class="temp-warn-note">Confirmed by phone and in-person July 5. Fixed July 6.</span></li>
				<li>Splash Wave Pool was unexpectedly closed on Friday, July 3. It is not clear whether this is a one-off occurrence. <span class="temp-warn-note">Confirmed by phone and in-person July 3. Fixed July 6.</span></li>
				<li>The skating at Ray Friel on Sunday is from 3-5pm, not 5-6. <span class="temp-warn-note">Confirmed by phone and in-person July 5. Fixed July 6.</span></li>
			</ul>
			<p>If you notice any other completely incorrect times, please report it to the city. You can also use the feedback button on the bottom-right of this page to send me a message so I can note it here too.</p>
		</div>
	</aside>
}

const tempWarnCSS = `<style>
.temp-warn {
	position: relative;
	max-width: 44rem;
	margin: 1rem 0;
	padding: .75rem 1rem;
	border: 1px solid light-dark(rgb(32 94 166 / .35), rgb(131 173 234 / .35));
	border-radius: .5rem;
	background: light-dark(rgb(32 94 166 / .08), rgb(131 173 234 / .12));
	color: var(--fxki-tx);
	font-size: .9rem;
	line-height: 1.4;
}
.temp-warn-head {
	display: flex;
	align-items: center;
	gap: .4rem;
}
.temp-warn-head h2 {
	margin: 0;
	font-size: .95rem;
	font-weight: 600;
	color: var(--fxki-bl);
}
.temp-warn-ico {
	flex: none;
	color: var(--fxki-bl);
}
.temp-warn-ico::before {
	font-family: 'Material Symbols Outlined';
	content: '\e88e'; /* info */
	font-size: 1.2rem;
	line-height: 1;
}
.temp-warn-body > :first-child { margin-top: .5rem; }
.temp-warn-body > :last-child { margin-bottom: 0; }
.temp-warn-body p { margin: .5rem 0; }
.temp-warn-body ul { margin: .5rem 0; padding-left: 1.25rem; }
.temp-warn-body li { margin: .15rem 0; }
.temp-warn-note { display: block; font-size: .8rem; color: var(--fxki-tx-2); }
.temp-warn-close {
	position: absolute;
	top: .35rem;
	right: .5rem;
	padding: .1rem .35rem;
	border: none;
	background: none;
	color: var(--fxki-tx-2);
	font: inherit;
	font-size: 1.3rem;
	line-height: 1;
	cursor: pointer;
}
.temp-warn-close:hover { color: var(--fxki-tx); }
.temp-warn-dismissable {
	position: absolute;
	left: .65rem;
	right: .65rem;
	bottom: .65rem;
	z-index: 1000;
	max-width: 30rem;
	margin: 0;
	padding-right: 2rem;
	max-height: 45%;
	overflow-y: auto;
	background: linear-gradient(light-dark(rgb(32 94 166 / .08), rgb(131 173 234 / .12)), light-dark(rgb(32 94 166 / .08), rgb(131 173 234 / .12))), var(--fxki-bg);
}
/* on mobile, clear the collapsed facilities sheet toggle at the bottom (when
 * the sheet is open it covers the map bottom and hides this anyway) */
@media (max-width: 900px) {
	.temp-warn-dismissable { bottom: 2.9rem; }
}
</style>`
